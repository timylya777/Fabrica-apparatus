package com.fabrica.me;

/**
 * Маркер-интерфейс узла-коннектора ME-сети (например, ME-сетка): участвует
 * в обходе сети, но не владеет собственным хранилищем — его getMeStorage()
 * пересчитывает хранилище всей сети заново. Такие узлы нельзя опрашивать
 * во время обхода, иначе два коннектора рядом вызовут бесконечную рекурсию.
 */
public interface MeNetworkConnector {
}
